# File Manager Workspace

Master spec §49.

## Inventory

- SCR-FILES-001 Main: ← folder ⋮ · search · files · grid/list switch.
- SCR-FILES-002 Selection mode: toolbar Share/Copy/Move/Rename/Delete/
  More (long-press entry).
- SCR-FILES-003 Search: query + grouped results.
- SCR-FILES-004 Details: metadata + Open with / AI actions.
- SCR-FILES-005 Operations: copy/move progress + conflicts + errors.

## Rules

Each item: icon/thumbnail + filename + metadata (§49). Empty: "No files
here" + [New folder] (+ AI hint). Errors explain + [Retry]. All ops via
files.* tools (audited). AI command example: "หาไฟล์ APK ล่าสุด" (§80).
