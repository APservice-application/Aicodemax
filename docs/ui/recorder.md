# Recorder (Screen Recorder workspace)

Master spec (Recorder application §1; honest capability states).

## Inventory

- SCR-REC-001 Main: mode tabs (Screen / Audio / Camera) + output list.
- SCR-REC-002 Screen: ●/■ + permission + output (or honest
  unsupported state with system-recorder guidance — no fake UI).
- SCR-REC-003 Audio: record + teleprompter (existing engine).
- SCR-REC-004 Camera: system capture → import to project.
- SCR-REC-005 Outputs: recordings + Open/Share/Delete + import.

Background recording keeps state in Tasks (§62). Permissions follow
§67 PERMISSION_REQUIRED with explanation + settings action.
