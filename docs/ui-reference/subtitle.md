# UI Reference — Subtitle

## Reference applications
CapCut captions, VN subtitles, Aegisub (layout concepts only).

## Observed
- Layout: preview · cue list · timing controls · style.
- Navigation: project → cues → export/burn.
- Controls: add/edit/delete cue, shift timing, styles, translate.
- Interaction: tap cue → edit timing/text; waveform-assisted sync (desktop).
- Responsive: list-first on narrow; preview-first on wide.

## Adopt
Cue list + timing shift + style + translate + burn-in flow.

## Change
Engine = subtitle.* tools (make/parse/shift/translate/burn); honest
no-STT state until an engine exists; AI chain "สร้างซับให้วิดีโอนี้".
